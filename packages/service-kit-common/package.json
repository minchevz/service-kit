{
  "name": "@service-kit/common",
  "version": "2.33.2",
  "description": "Service Kit common types and utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b --pretty",
    "watch": "yarn build --watch",
    "test": "jest --detectOpenHandles --no-cache --colors --coverage",
    "test:watch": "yarn test --watchAll",
    "lintify": "eslint '**/*.{js,ts}' --fix"
  },
  "devDependencies": {
    "jest": "^29.2.2",
    "ts-jest": "^29.0.3",
    "typescript": "4.8.3"
  }
}
