{
  "CACHE2": {
    "CACHE_PREFIX": {
      "doc": "[Feature] sample.",
      "default": "library-redis",
      "env": "CACHE_PREFIX"
    },
    "REDIS_MONITOR": {
      "doc": "[Feature] sample.",
      "default": false,
      "env": "REDIS_MONITOR"
    },
    "CACHE_TTL": {
      "doc": "[Feature] sample.",
      "default": null,
      "env": "CACHE_TTL"
    },
    "REDIS": {
      "REDIS_NAME": {
        "doc": "[Feature] name.",
        "default": "gms_redis",
        "env": "REDIS_NAME"
      },
      "REDIS_HOST": {
        "doc": "[Feature] host.",
        "default": "127.0.0.1",
        "env": "REDIS_HOST"
      },
      "REDIS_PORT": {
        "doc": "[Feature] port.",
        "default": 6379,
        "env": "REDIS_PORT"
      },
      "REDIS_PASSWORD": {
        "doc": "[Feature] password.",
        "default": null,
        "env": "REDIS_PASSWORD"
      },
      "REDIS_MAX_RETRIES_PER_REQUEST": {
        "doc": "[Feature] maxRetriesPerRequest.",
        "default": 1,
        "env": "REDIS_MAX_RETRIES_PER_REQUEST"
      },
      "REDIS_ENABLE_OFFLINE_QUEUE": {
        "doc": "[Feature] enableOfflineQueue.",
        "default": false,
        "env": "REDIS_ENABLE_OFFLINE_QUEUE"
      },
      "SENTINEL_HOST_0": {
        "doc": "[Feature] sentinel host 0.",
        "default": "",
        "env": "REDIS_SENTINEL_HOST_0"
      }
    }
  },
  "CACHE": {
    "CACHE_PREFIX": {
      "doc": "[Feature] sample.",
      "default": "library-node",
      "env": "CACHE_PREFIX"
    },
    "CACHE_TTL": {
      "doc": "[Feature] sample.",
      "default": 10,
      "env": "CACHE_TTL"
    },
    "MEMORY": {
      "stdTTL": {
        "doc": "[Feature] stdTTL.",
        "default": 0,
        "env": "NODE_MEMORY_STD_TTL"
      },
      "checkperiod": {
        "doc": "[Feature] checkperiod.",
        "default": 100,
        "env": "NODE_MEMORY_CHECK_PERIOD"
      },
      "maxKeys": {
        "doc": "[Feature] maxKeys.",
        "default": -1,
        "env": "NODE_MEMORY_MAX_KEYS"
      },
      "useClones": {
        "doc": "[Feature] useClones.",
        "default": true,
        "env": "NODE_MEMORY_USE_CLONES"
      }
    }
  }
}
